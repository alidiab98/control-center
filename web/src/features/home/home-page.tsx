/** Placeholder until F1 builds the attention queue and the agents list. */
export function HomePage() {
  return (
    <section className="px-6 pb-6">
      <h1 className="text-lg font-semibold text-text">Needs you</h1>
      <p className="mt-2 text-sm text-muted">The attention queue arrives in F1.</p>
    </section>
  )
}
